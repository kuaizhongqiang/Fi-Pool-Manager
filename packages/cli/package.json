{
  "name": "@fi-pool/cli",
  "version": "0.1.0",
  "private": true,
  "description": "Fi-Pool-Manager CLI 命令行入口",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "fi-pool": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "dotenv": "^16.4.0",
    "@fi-pool/server": "*"
  },
  "devDependencies": {}
}
